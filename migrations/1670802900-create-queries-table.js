export async function up(sql) {
  await sql`
    CREATE TABLE queries (
      id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
      user_id integer NOT NULL,
      question varchar(140) NOT NULL,
      alternative_1 varchar(70) NOT NULL,
      alternative_2 varchar(70) NOT NULL,
      alternative_3 varchar(70),
      alternative_4 varchar(70),
      alternative_5 varchar(70),
      quality_1 varchar(70) NOT NULL,
      quality_2 varchar(70) NOT NULL,
      quality_3 varchar(70) NOT NULL,
      quality_4 varchar(70),
      quality_5 varchar(70),
      quality_6 varchar(70),
      quality_7 varchar(70)
    )
  `;
}

export async function down(sql) {
  await sql`
  DROP TABLE queries
`;
}
